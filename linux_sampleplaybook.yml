- name: Sample Playbook linux
  hosts: all
  gather_facts: false
  tasks:
     - name: ping test
       ping:
       register: ping_op
     - debug:
         var: ping_op
     - name: copy file
       shell:
           |
         sar -u 1 3
       register: s_op
     - debug: 
          var: s_op
